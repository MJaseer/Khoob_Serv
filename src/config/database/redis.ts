// import redis from "redis";
// const client = redis.createClient({url:process.env.REDIS_PORT});

// client.on('connect', () => {
//     console.log('Connected to Redis server');
// });

// client.on('error', (err) => {
//     console.log('Redis error:', err);
// });